  ---

  todo sujet correct et vrac :

- ajouter cgi
  - function to detect cgi (is_cgi)
  - handles GET cgi
  - function to set cgi env
- handles post cgi
- ajouter cookies
- same port multiple times should not work
- A request to your server should never hang forever.
- careful aux stoi
- no leaks